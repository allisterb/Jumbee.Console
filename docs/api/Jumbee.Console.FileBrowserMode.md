# <a id="Jumbee_Console_FileBrowserMode"></a> Enum FileBrowserMode

Namespace: [Jumbee.Console](Jumbee.Console.md)  
Assembly: Jumbee.Console.dll  

What a <xref href="Jumbee.Console.FileBrowser" data-throw-if-not-resolved="false"></xref> is asking the user to choose.

```csharp
public enum FileBrowserMode
```

## Fields

`OpenFile = 0` 

A file. Directories are navigated into rather than chosen.



`OpenDirectory = 1` 

A directory. Files are listed for context but cannot be chosen.



